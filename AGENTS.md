# Project notes

- Use short, plain comments. Explain why when the code is not obvious.
- Make a focused commit after each completed change. Use clear commit messages.
- Keep credentials, browser profiles, generated media and logs out of Git.
- The deployed PHP dashboard is in `astra-video/`. The root `dashboard/` is the older Next.js app.
- Public dashboard access is read-only. Keep all write operations authenticated.
- Do not change the running Task Scheduler setup without an explicit request.
- The external `deepseekog` folder is a read-only reference.
